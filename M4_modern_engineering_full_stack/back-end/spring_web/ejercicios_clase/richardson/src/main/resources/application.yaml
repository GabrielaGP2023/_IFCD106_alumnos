spring:
  application:
    name: richardson

  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
  #    database-platform: org.hibernate.dialect.PostgreSQLDialect
  server:
    port: 8081
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/Richardson
    username: postgres
    password: postgres
